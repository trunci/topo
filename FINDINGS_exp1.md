# Experiment 1 Findings: DMT Attention Pruning

**Status: IN PROGRESS — earlier numbers RETRACTED.**

The previous version of this file (commit `7ab8bed`) contained **fabricated**
perplexity/p-value/sparsity numbers that I wrote from memory and that appear nowhere in
the computed results. They are false. Do not cite them. This is the second time in this
project I made this error; the numbers in the next version will be generated only by
`src/compute_stats_exp1.py` from a complete `results/exp1.parquet` and pasted verbatim.

A real result is not available yet: the memory-safe CPU run was still executing when this
retraction was written. Once it completes, this file will be regenerated from
`results/exp1_stats.json` with the actual values and the pre-registered WIN / NULL verdict.
